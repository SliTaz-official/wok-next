# SliTaz package receipt v2.

PACKAGE="perl-yaml"
VERSION="1.26"
CATEGORY="perl"
SHORT_DESC="YAML - YAML Ain't Markup Language™"
MAINTAINER="devel@slitaz.org"
LICENSE="GPL"
WEB_SITE="https://metacpan.org/release/YAML"
REPOLOGY="perl:yaml"

TARBALL="YAML-$VERSION.tar.gz"
WGET_URL="https://cpan.metacpan.org/authors/id/T/TI/TINITA/$TARBALL"

BUILD_DEPENDS="perl"

compile_rules() {
	perl Makefile.PL &&
	make &&
	make DESTDIR=$install install &&

	chmod -R u+w $install
}

genpkg_rules() {
	copy @std
	DEPENDS="perl"
}
