# SliTaz package receipt v2.

PACKAGE="perl-io-captureoutput"
VERSION="1.1104"
CATEGORY="perl"
SHORT_DESC="IO::CaptureOutput - capture STDOUT and STDERR from Perl code, \
subprocesses or XS"
MAINTAINER="al.bobylev@gmail.com"
LICENSE="GPL"
WEB_SITE="https://metacpan.org/release/IO-CaptureOutput"
REPOLOGY="perl:io-captureoutput"

TARBALL="IO-CaptureOutput-$VERSION.tar.gz"
WGET_URL="https://cpan.metacpan.org/authors/id/D/DA/DAGOLDEN/$TARBALL"

BUILD_DEPENDS="perl"

compile_rules() {
	perl Makefile.PL &&
	make &&
	make DESTDIR=$install install &&

	chmod -R u+w $install
}

genpkg_rules() {
	copy @std
	DEPENDS="perl"
}
