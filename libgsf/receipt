# SliTaz package receipt.

PACKAGE="libgsf"
VERSION="1.14.40"
CATEGORY="libs"
SHORT_DESC="GNOME Structured File Library"
MAINTAINER="pankso@slitaz.org"
LICENSE="GPL2"
TARBALL="$PACKAGE-$VERSION.tar.xz"
WEB_SITE="https://developer.gnome.org/gsf/"
WGET_URL="$GNOME_MIRROR/$PACKAGE/${VERSION%.*}/$TARBALL"

DEPENDS="bzlib gdk-pixbuf glib glibc-base libffi libpng libxml2 pcre zlib"
BUILD_DEPENDS="gtk-doc glib-dev libxml2-dev zlib-dev gdk-pixbuf-dev"

# Rules to configure and make the package.
compile_rules()
{
	./configure $CONFIGURE_ARGS && make && make install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	copy gsf *.so* gsf-vba* gsf-office*
	rm -r $fs/usr/share/man
}
