# SliTaz package receipt v2.

PACKAGE="xorg-rgb"
VERSION="1.0.6"
CATEGORY="x-window"
SHORT_DESC="X colorname to RGB mapping database"
MAINTAINER="devel@slitaz.org"
LICENSE="MIT"
WEB_SITE="https://www.x.org/wiki/"
REPOLOGY="rgb"

TARBALL="rgb-$VERSION.tar.bz2"
WGET_URL="$XORG_MIRROR/app/$TARBALL"

BUILD_DEPENDS="xorg-xorgproto"

compile_rules() {
	touch /root/missing

	./configure $CONFIGURE_ARGS &&
	make &&
	make install
}

genpkg_rules() {
	copy @std
}
