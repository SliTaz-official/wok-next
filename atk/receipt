# SliTaz package receipt v2.

PACKAGE="atk"
VERSION="2.30.0"
CATEGORY="x-window"
SHORT_DESC="Accessibility toolkit"
MAINTAINER="devel@slitaz.org"
LICENSE="GPL2"
WEB_SITE="https://developer.gnome.org/atk/"
LFS="http://www.linuxfromscratch.org/blfs/view/svn/x/atk.html"

TARBALL="$PACKAGE-$VERSION.tar.xz"
WGET_URL="$GNOME_MIRROR/$PACKAGE/${VERSION%.*}/$TARBALL"
TARBALL_SHA1="08a1c0e5173b992fd8b59cd7c40c3ae0cf0dc4f3"

BUILD_DEPENDS="meson ninja glib-dev gobject-introspection-dev gettext-dev"
SPLIT="$PACKAGE-dev"

DEPENDS_std="glib"
DEPENDS_dev="$PACKAGE glib-dev"

TAGS_std="accessibility"

compile_rules() {
	mkdir build
	cd    build

	meson-wrapper &&
	ninja &&
	ninja install
}
