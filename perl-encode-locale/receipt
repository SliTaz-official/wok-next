# SliTaz package receipt v2.

PACKAGE="perl-encode-locale"
VERSION="1.05"
CATEGORY="perl"
SHORT_DESC="Encode::Locale - Determine the locale encoding"
MAINTAINER="devel@slitaz.org"
LICENSE="GPL"
WEB_SITE="https://metacpan.org/release/Encode-Locale"
REPOLOGY="perl:encode-locale"

TARBALL="Encode-Locale-$VERSION.tar.gz"
WGET_URL="https://cpan.metacpan.org/authors/id/G/GA/GAAS/$TARBALL"

BUILD_DEPENDS="perl"

compile_rules() {
	perl Makefile.PL &&
	make &&
	make DESTDIR=$install install &&

	chmod -R u+w $install
}

genpkg_rules() {
	copy @std
	DEPENDS="perl"
}
