# SliTaz package receipt v2.

PACKAGE="lftp"
VERSION="4.8.3"
CATEGORY="network"
SHORT_DESC="Small but powerful FTP client"
MAINTAINER="mimas@slitaz.org"
LICENSE="GPL3"
WEB_SITE="http://lftp.yar.ru/"

TARBALL="$PACKAGE-$VERSION.tar.bz2"
WGET_URL="${WEB_SITE}ftp/$TARBALL"

BUILD_DEPENDS="gmp-dev ncurses-dev readline-dev expat-dev gperf gnutls-dev"

CROSS="bug: failed at test"

compile_rules() {
	./configure \
		--with-gnutls \
		--without-openssl \
		$CONFIGURE_ARGS &&
	fix libtool &&
	make &&
	make install
}

genpkg_rules() {
	copy @std
	DEPENDS="gmp readline expat gnutls gcc-lib-base"
}
