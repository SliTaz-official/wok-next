# SliTaz package receipt v2.

PACKAGE="mousepad"
VERSION="0.4.0"
CATEGORY="x-window"
SHORT_DESC="Xfce text editor"
MAINTAINER="erjo@slitaz.org"
LICENSE="GPL2"
WEB_SITE="http://www.xfce.org/"

TARBALL="$PACKAGE-$VERSION.tar.bz2"
WGET_URL="http://archive.xfce.org/src/apps/$PACKAGE/${VERSION%.*}/$TARBALL"

BUILD_DEPENDS="intltool gtk+-dev gtksourceview-dev dbus-glib-dev"

compile_rules() {
	./configure \
		--disable-debug \
		$CONFIGURE_ARGS &&
	fix libtool &&
	make &&
	make install
}

genpkg_rules() {
	copy @std
	DEPENDS="dbus dbus-glib glib gtk+ gtksourceview pango"
	TAGS="Xfce text-editor"
}
