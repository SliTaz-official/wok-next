# SliTaz package receipt v2.

PACKAGE="duplicity"
VERSION="0.6.20"
CATEGORY="network"
SHORT_DESC="Encrypted bandwidth-efficient backup using the rsync algorithm"
MAINTAINER="pascal.bellard@slitaz.org"
LICENSE="GPL2"
WEB_SITE="http://duplicity.nongnu.org/"

TARBALL="$PACKAGE-$VERSION.tar.gz"
WGET_URL="https://code.launchpad.net/$PACKAGE/${VERSION%.*}-series/$VERSION/+download/$TARBALL"

BUILD_DEPENDS="python-dev librsync-dev gnupg python-paramiko cryptopp lftp"
# NcFTP boto librsync-dev

compile_rules() {
	python -B setup.py install --root=$install
}

genpkg_rules() {
	copy @std
	DEPENDS="python librsync-dev gnupg python-paramiko cryptopp lftp"
}
