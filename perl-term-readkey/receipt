# SliTaz package receipt v2.

PACKAGE="perl-term-readkey"
VERSION="2.37"
CATEGORY="perl"
SHORT_DESC="Term::ReadKey - A perl module for simple terminal control"
MAINTAINER="devel@slitaz.org"
LICENSE="GPL"
WEB_SITE="https://metacpan.org/release/TermReadKey"
REPOLOGY="perl:term-readkey"

TARBALL="TermReadKey-$VERSION.tar.gz"
WGET_URL="https://cpan.metacpan.org/authors/id/J/JS/JSTOWE/$TARBALL"

BUILD_DEPENDS="perl-dev"

compile_rules() {
	perl Makefile.PL &&
	make &&
	make DESTDIR=$install install &&

	chmod -R u+w $install
}

genpkg_rules() {
	copy @std
	DEPENDS="perl"
}
