# SliTaz package receipt.

PACKAGE="perl-gssapi"
VERSION="0.28"
CATEGORY="perl"
SHORT_DESC="GSSAPI - Perl extension providing access to the GSSAPIv2 library"
MAINTAINER="pascal.bellard@slitaz.org"
LICENSE="GPL"
WEB_SITE="https://metacpan.org/release/GSSAPI"
REPOLOGY="perl:gssapi"

TARBALL="GSSAPI-$VERSION.tar.gz"
WGET_URL="https://cpan.metacpan.org/authors/id/A/AG/AGROLMS/$TARBALL"

DEPENDS="libcomerr3 libkrb5 perl"
BUILD_DEPENDS="perl-dev krb5-dev"

# Rules to configure and make the package.
compile_rules()
{
	perl Makefile.PL &&
	make && make DESTDIR=$install install &&

	chmod -R u+w $install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	copy @std
}
