# SliTaz package receipt.

PACKAGE="perl-dbd-sqlite"
VERSION="1.54"
CATEGORY="perl"
SHORT_DESC="DBD::SQLite - Self-contained RDBMS in a DBI Driver"
MAINTAINER="admin@trixarian.net"
LICENSE="GPL"
WEB_SITE="https://metacpan.org/release/DBD-SQLite"

TARBALL="DBD-SQLite-$VERSION.tar.gz"
WGET_URL="https://cpan.metacpan.org/authors/id/I/IS/ISHIGAKI/$TARBALL"

# FIXME: sqlite-dev and libsqlite aren't detected as (b)deps
DEPENDS="perl-dbi libsqlite"
BUILD_DEPENDS="$DEPENDS sqlite-dev"

# Rules to configure and make the package.
compile_rules()
{
	perl Makefile.PL &&
	make && make DESTDIR=$install install &&

	chmod -R u+w $install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	copy @std @dev
}
