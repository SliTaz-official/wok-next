# SliTaz package receipt v2.

PACKAGE="perl-timedate"
VERSION="2.30"
CATEGORY="perl"
SHORT_DESC="Date formating subroutines"
MAINTAINER="al.bobylev@gmail.com"
LICENSE="GPL"
WEB_SITE="https://metacpan.org/release/TimeDate"
REPOLOGY="perl:timedate"

TARBALL="TimeDate-$VERSION.tar.gz"
WGET_URL="https://cpan.metacpan.org/authors/id/G/GB/GBARR/$TARBALL"

BUILD_DEPENDS="perl"

compile_rules() {
	perl Makefile.PL &&
	make &&
	make DESTDIR=$install install &&

	chmod -R u+w $install
}

genpkg_rules() {
	copy @std
	DEPENDS="perl"
}
