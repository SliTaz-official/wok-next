# SliTaz package receipt v2.

PACKAGE="drbd-utils"
VERSION="9.11.0"
CATEGORY="system-tools"
SHORT_DESC="Block device to build high availability clusters"
MAINTAINER="pascal.bellard@slitaz.org"
LICENSE="GPL2"
WEB_SITE="https://www.linbit.com/en/drbd-community/"

TARBALL="$PACKAGE-$VERSION.tar.gz"
#WGET_URL="http://oss.linbit.com/drbd/$TARBALL"
WGET_URL="https://github.com/LINBIT/$PACKAGE/archive/v$VERSION.tar.gz"

BUILD_DEPENDS="flex libxslt"

compile_rules()
{
	./configure $CONFIGURE_ARGS &&
	make &&
	make DESTDIR=$install install
}

genpkg_rules()
{
	copy @std
}
