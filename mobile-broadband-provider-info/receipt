# SliTaz package receipt v2.

PACKAGE="mobile-broadband-provider-info"
VERSION="20120614"
CATEGORY="network"
SHORT_DESC="Service Provider Database"
MAINTAINER="domcox@slitaz.org"
LICENSE="cc-pd"
WEB_SITE="http://live.gnome.org/NetworkManager/MobileBroadband/ServiceProviders"

TARBALL="$PACKAGE-$VERSION.tar.xz"
WGET_URL="http://ftp.acc.umu.se/pub/GNOME/sources/$PACKAGE/$VERSION/$TARBALL"

SPLIT="mobile-broadband-provider-info-dev"

compile_rules() {
	./configure $CONFIGURE_ARGS && make && make install
}

genpkg_rules() {
	case $PACKAGE in
		mobile-broadband-provider-info)
			mkdir -p $fs/usr/share
			cp -a $install/usr/share/$PACKAGE $fs/usr/share
			;;
		*-dev)
			mkdir -p $fs/usr/lib
			cp -a $install/usr/share/pkgconfig $fs/usr/lib
			;;
	esac
}
