# SliTaz package receipt.

PACKAGE="perl-unicode-maputf8"
VERSION="1.11"
CATEGORY="perl"
SHORT_DESC="Unicode::MapUTF8 - Conversions to and from arbitrary character \
sets and UTF8"
MAINTAINER="pascal.bellard@slitaz.org"
LICENSE="GPL"
WEB_SITE="https://metacpan.org/release/Unicode-MapUTF8"
REPOLOGY="perl:unicode-maputf8"

TARBALL="Unicode-MapUTF8-$VERSION.tar.gz"
WGET_URL="https://cpan.metacpan.org/authors/id/S/SN/SNOWHARE/$TARBALL"

DEPENDS="perl-jcode perl-unicode-map perl-unicode-map8 perl-unicode-string"
BUILD_DEPENDS="$DEPENDS"

# Rules to configure and make the package.
compile_rules()
{
	perl Makefile.PL &&
	make && make DESTDIR=$install install &&

	chmod -R u+w $install
}

# Rules to gen a SliTaz package suitable for Tazpkg.
genpkg_rules()
{
	copy @std
}
