# SliTaz package receipt v2.

PACKAGE="libsndfile"
VERSION="1.0.28"
CATEGORY="multimedia"
SHORT_DESC="Reading and writing files containing sampled sound"
MAINTAINER="devel@slitaz.org"
LICENSE="LGPL2.1"
WEB_SITE="http://www.mega-nerd.com/libsndfile/"

TARBALL="$PACKAGE-$VERSION.tar.gz"
WGET_URL="$WEB_SITE/files/$TARBALL"

BUILD_DEPENDS="alsa-lib-dev libvorbis-dev flac-dev"
SPLIT="$PACKAGE-apps $PACKAGE-dev"

compile_rules() {
	./configure \
		--disable-static \
		$CONFIGURE_ARGS &&
	fix libtool &&
	make &&
	make install
}

genpkg_rules() {
	case $PACKAGE in
		libsndfile)
			copy *.so*
			DEPENDS="flac libogg libvorbis"
			;;
		*-apps)
			copy bin/
			CAT="multimedia|applications"
			DEPENDS="alsa-lib libsndfile"
			;;
		*-dev)
			copy @dev
			DEPENDS="libsndfile libsndfile-apps flac-dev libogg-dev \
			libvorbis-dev"
			;;
	esac
}
